import React from 'react';
import QuizContainer from '@/components/QuizContainer';

const Home: React.FC = () => {
  return (
    <div className="py-4">
      <QuizContainer />
    </div>
  );
};

export default Home;
