import React from 'react';
import { Heart, Sparkles } from 'lucide-react';

interface PDFPreviewProps {
  firstName: string;
  advice: string;
}

const PDFPreview: React.FC<PDFPreviewProps> = ({ firstName, advice }) => {
  const currentDate = new Date().toLocaleDateString('en-US', { 
    year: 'numeric', 
    month: 'long', 
    day: 'numeric' 
  });
  
  // Split advice into paragraphs
  const paragraphs = advice.split('\n\n');

  return (
    <div className="border border-pink-100 rounded-2xl p-5 md:p-7 mb-6 bg-white shadow-md transition-all duration-300 hover:shadow-lg">
      {/* Decorative Elements */}
      <div className="absolute -top-3 -right-3 w-24 h-24 bg-pink-50 rounded-full opacity-30 blur-xl -z-10"></div>
      <div className="absolute -bottom-3 -left-3 w-24 h-24 bg-blue-50 rounded-full opacity-30 blur-xl -z-10"></div>
      
      <div className="flex justify-between items-center mb-6 border-b border-pink-100 pb-4">
        <div>
          <h3 className="font-script text-2xl bg-gradient-to-r from-pink-500 to-blue-500 bg-clip-text text-transparent">Your Custom Obsession Trigger Plan</h3>
          <p className="text-xs text-neutral-600">Prepared specially for <span className="font-medium text-pink-600">{firstName}</span></p>
        </div>
        <div>
          <div className="w-12 h-12 bg-gradient-to-r from-pink-100 to-blue-100 rounded-full flex items-center justify-center shadow-sm">
            <Heart className="text-pink-500 h-6 w-6 fill-pink-100" />
          </div>
        </div>
      </div>
      
      {/* AI-Generated Content Preview - Show just a sample with blur effect */}
      <div className="prose max-w-none mb-6 relative overflow-hidden" style={{ maxHeight: '300px' }}>
        {paragraphs.slice(0, 2).map((paragraph, index) => (
          <p key={index} className="mb-4 text-neutral-700">{paragraph}</p>
        ))}
        
        {/* Blur effect for content that continues in the PDF */}
        {paragraphs.length > 2 && (
          <>
            <div className="absolute bottom-0 left-0 right-0 h-40 bg-gradient-to-t from-white to-transparent"></div>
            <div className="text-center text-sm text-blue-500 absolute bottom-0 left-0 right-0">
              <Sparkles className="inline-block h-4 w-4 mr-1" /> Full advice available in your PDF
            </div>
          </>
        )}
      </div>
      
      {/* Call to Action */}
      <div className="bg-gradient-to-r from-pink-50 to-blue-50 p-5 rounded-xl border border-pink-100 mb-4">
        <p className="text-sm text-neutral-700 font-medium">To learn the full system that activates his Hero Instinct, download <span className="font-bold bg-gradient-to-r from-pink-500 to-blue-500 bg-clip-text text-transparent">His Secret Obsession</span>.</p>
        <p className="text-sm text-pink-600 font-medium mt-2">Click here 👉 [Affiliate Link]</p>
      </div>
      
      <div className="text-xs text-neutral-500 text-center">
        Your custom plan generated by Obsession Trigger on {currentDate}
      </div>
    </div>
  );
};

export default PDFPreview;
